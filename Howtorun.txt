✅ What's in the ZIP (47 files, 576KB)
🗂️ Complete Folder Structure
FolderContentsdatabricks/notebooks/5 PySpark notebooks — bronze metadata, bronze→silver, silver→gold, DQ engine, validation rulesterraform/main.tf, variables.tf, outputs.tf + 6 modules (storage, network, databricks, synapse, keyvault, monitoring)sql/Synapse DDL (fact + 3 dims), Gold loader, 5 analytics views with window functionstests/52 unit + DQ tests — all passing ✅pipelines/adf/ADF pipeline JSON + linked services configsample-data/Generator script + 5 CSV files (12,050 total rows with deliberate nulls/dupes)docs/Architecture (Mermaid), Security, Cost Optimisation, Disaster Recoverypowerbi/Dashboard spec with DAX measures + semantic model JSONmonitoring/KQL Log Analytics queries + ARM alert rules.github/workflows/Full CI/CD — Dev → Test → Prod with approval gates

🚀 How to Run Locally
bash# macOS/Linux
./setup_local.sh

# Windows
setup_local.bat
Or manually:
bashpython -m venv .venv && source .venv/bin/activate
pip install -r requirements-dev.txt
cd sample-data && python generate_data.py && cd ..
pytest tests/unit/ tests/data_quality/ -v
📤 Push to GitHub
bashgit init && git add . && git commit -m "feat: Azure Healthcare Analytics Platform"
gh repo create azure-healthcare-analytics --public --push
# Then add the 7 GitHub Secrets listed in README → CI/CD will handle the rest